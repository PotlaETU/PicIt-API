spring:
  profiles:
    active: env

  application:
    name: picit-api
  data:
    mongodb:
      database: PicIt

server:
  port: 8081

security:
  jwt:
    secret-key: ${JWT_SECRET_KEY:secret-key}
    expiration-time: ${JWT_EXPIRATION_TIME:60000}
    refresh-time: ${JWT_REFRESH_TIME:2000000}